# Validated against schemas/eval-suite.schema.json.
name: embedding
description: Portable text-embedding evaluation for every certified profile
checks:
  - type: runtime-readiness
  - type: text-embedding
    params: {target: runtime}
  - type: text-embedding
    id: gateway-text-embedding
    params: {target: gateway}
  - type: pgvector-roundtrip
    params: {target: gateway}
